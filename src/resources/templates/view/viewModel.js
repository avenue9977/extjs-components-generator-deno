Ext.define('App.<%= location %>.<%= name %>ViewModel', {
    extend: 'Ext.app.ViewModel',
    alias: 'viewmodel.<%= xtype %>ViewModel',

    requires: [
        // Require needed files
    ],

    stores: [
        // Add stores
    ],

    data: {
        // Add data
    },

    formulas: {
        // Add formulas
    }
});
